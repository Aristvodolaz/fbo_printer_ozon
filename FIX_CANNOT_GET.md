# Исправление ошибки "Cannot GET /"

## Проблема
При открытии `http://10.171.12.36:3019` появляется ошибка "Cannot GET /"

## Решение

### Шаг 1: Перезапустите сервер

Остановите текущий сервер (Ctrl+C) и запустите заново:

```bash
npm start
```

Или если используете `start.bat`:
- Закройте окно с сервером
- Запустите `start.bat` снова

### Шаг 2: Проверьте логи

При запуске сервера вы должны увидеть:
```
🚀 Сервер запущен на порту 3019
🌐 Приложение доступно:
   - Локально: http://localhost:3019
   - В сети: http://10.171.12.36:3019
```

При запросе к главной странице в консоли должно появиться:
```
Отправка файла: C:\путь\к\проекту\index.html
```

### Шаг 3: Проверьте наличие файлов

Убедитесь, что в папке проекта есть файл `index.html`:
- Если файла нет, скопируйте `сканер_озон —2.html` и переименуйте в `index.html`
- Или сервер автоматически попытается использовать оригинальный файл

### Шаг 4: Проверьте доступ

Откройте в браузере:
```
http://10.171.12.36:3019
```

Если все еще не работает, попробуйте:
```
http://10.171.12.36:3019/index.html
```

### Шаг 5: Альтернативное решение

Если проблема сохраняется, можно напрямую открыть файл:
1. Найдите файл `сканер_озон —2.html` в папке проекта
2. Откройте его в браузере (двойной клик)
3. Но учтите: без сервера сохранение в БД работать не будет

## Что было исправлено

1. ✅ Добавлена проверка существования файлов
2. ✅ Добавлена обработка ошибок
3. ✅ Добавлена поддержка оригинального файла как резервного варианта
4. ✅ Улучшено логирование для отладки

## Если проблема сохраняется

Проверьте:
1. Запущен ли сервер? (должен быть процесс node.exe)
2. Правильный ли порт? (3019)
3. Есть ли файл index.html в папке проекта?
4. Что показывает консоль сервера при запросе?
