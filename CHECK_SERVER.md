# Проверка работы сервера

## Ваш сервер запущен на:
**http://10.171.12.36:3019**

## Проверка работы:

### 1. Проверка главной страницы
Откройте в браузере:
```
http://10.171.12.36:3019
```
Должна открыться страница приложения для печати маркировок.

### 2. Проверка API
Откройте в браузере:
```
http://10.171.12.36:3019/api/health
```
Должен вернуться JSON:
```json
{
  "success": true,
  "status": "ok",
  "timestamp": "2026-02-20T..."
}
```

### 3. Проверка подключения к БД
Если сервер запущен, проверьте консоль - должно быть сообщение:
- ✅ `Подключение к БД установлено` - если БД доступна
- ⚠️ `Предупреждение при создании таблицы` - если есть проблемы с таблицей (но это не критично)
- ❌ `Критическая ошибка подключения к БД` - если БД недоступна

## Использование:

1. **Откройте приложение:**
   ```
   http://10.171.12.36:3019
   ```

2. **Загрузите Excel файл** с привязкой (колонки: Маркировка и ВПС)
   - Название файла станет названием задания в БД

3. **Загрузите PDF файл** с маркировками

4. **Введите номер ВПС** и нажмите Enter
   - Маркировка найдется и отправится на печать
   - Прогресс автоматически сохранится в БД

5. **При работе с другого компьютера:**
   - Просто откройте `http://10.171.12.36:3019`
   - Загрузите тот же Excel файл (с тем же именем)
   - Прогресс автоматически загрузится из БД

## Устранение проблем:

### Не открывается страница
- Проверьте, что сервер запущен: `npm start`
- Проверьте файрвол на порт 3019
- Проверьте, что сервер слушает на `0.0.0.0`, а не только `localhost`

### API не отвечает
- Проверьте `http://10.171.12.36:3019/api/health`
- Проверьте логи сервера в консоли

### БД не подключается
- Проверьте доступность SQL Server с сервера
- Проверьте учетные данные в `db.js`
- Проверьте файрвол SQL Server

## Логи сервера

При запуске сервера вы увидите:
```
🚀 Сервер запущен на порту 3019
🌐 Приложение доступно:
   - Локально: http://localhost:3019
   - В сети: http://10.171.12.36:3019
📊 API доступен по адресу http://10.171.12.36:3019/api
```

Если все работает правильно, можете начинать использовать приложение!
